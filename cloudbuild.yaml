steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/gcpdevops:$SHORT_SHA', '.']

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/gcpdevops:$SHORT_SHA']

  # Fetch credentials for the target GKE cluster
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'container'
      - 'clusters'
      - 'get-credentials'
      - '${_CLUSTER_NAME}'
      - '--zone'
      - '${_CLUSTER_ZONE}'
      - '--project'
      - '$PROJECT_ID'

  # Apply the Kubernetes manifest (Deployment + Service)
  - name: 'gcr.io/cloud-builders/kubectl'
    args: ['apply', '-f', '../gke.yaml']
    env:
      - 'CLOUDSDK_COMPUTE_ZONE=${_CLUSTER_ZONE}'
      - 'CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}'

  # Point the deployment at the image just built
  - name: 'gcr.io/cloud-builders/kubectl'
    args: ['set', 'image', 'deployment/gcpdevops-app', 'gcpdevops=gcr.io/$PROJECT_ID/gcpdevops:$SHORT_SHA']
    env:
      - 'CLOUDSDK_COMPUTE_ZONE=${_CLUSTER_ZONE}'
      - 'CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}'

# Images to push to Container Registry after a successful build
images:
  - 'gcr.io/$PROJECT_ID/gcpdevops:$SHORT_SHA'

# Default values for the GKE cluster the app is deployed to.
# Override with --substitutions=_CLUSTER_NAME=...,_CLUSTER_ZONE=... or in the trigger config.
substitutions:
  _CLUSTER_NAME: your-gke-cluster
  _CLUSTER_ZONE: us-central1-a
